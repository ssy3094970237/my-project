git账户是ssy3094970237，密码1535284769y
2. 考核细则详解
A. 项目功能完整度(50%)
(1) 线上可访问(20%)：必须提部署链接。如果涉及后端，需确保接口能正常响应。
若因特殊原因后端无法部署，需提供本地启动视频录屏作为替代验证方式。
(2) 功能实现(30%)：前端至少3个独立路由，后端至少3个API接口（需提供接口文档或
Postman截图）。包含UI/UX体验，健壮性（可在功能实现中增加异常场景覆盖说明）
B. 工程规范与代码质量(25%)
(1) Git 提交历史(15%)：
 频率：整个开发周期内至少有3个不同日期的有效提交。
 内容：CommitMessage不能全是"update"或"fixbug"，必须有具体的描述（如"feat:add
login api", "fix: login page style"）。
 验证：如果一个项目代码量很大，但只有两三个Commit（比如全是最后一天提交的），直
接判定为不及格。
(2) 代码结构(5%)：代码目录清晰，符合Next.js和Flask的规范。
(3) 代码审查(5%)：利用AI（如Codex/Cursor/Trae等）进行一次CodeReview，并提交AI给出
的优化建议截图或报告。
C. AI 工具运用与文档(20%)
(1) Prompt 日志(10%)：
1要求每条Prompt必须附带AI返回的原始输出（可截屏或代码块）；
2要求标注该Prompt对应解决了哪个功能/哪个文件，便于对应代码审查。
(2) 项目文档(10%)：README.md必须包含项目介绍、技术栈、安装/运行指南、API文档。
D. 个人总结报告(5%)
单独提交一份不少于500字的总结。
内容需包括：遇到的最大困难及解决过程、对AI辅助编程的心得体会、个人在项目中的具
体贡献，鼓励深度思考。
2
3. 给学生的居家实训特别说明
(1) 取消组队，改为个人独立完成（或极简小组）：
建议：原本是3-4人一组，居家环境下沟通成本极高，且容易出现“搭便车”现象。强烈建议
改为个人独立完成，或者最多2人组队（且需明确分工）。
(2) 若坚持组队：必须在提交的文档中明确写出每个人的分工，且Git记录必须能对应上（例如
A负责前端，那么前端的Commit主要由A的账号提交）。
(3) 过程留痕是关键：
“我没有做，但我最后交了代码”在这一版方案中行不通。Git的提交记录就是你的考勤表。
(4) 必须产出“部署后的URL”：
必须把项目跑起来，提供一个可以点开就用的链接。
4.可选的加分项
(1) 项目实现了CI/CD（如GitHubActions自动部署）；
(2) 项目包含单元测试（如Jest/PyTest）；
(3) 项目使用了环境变量、日志、错误监控等工程化手段。
 完整提交物清单：项目GitHub仓库地址（公开仓库）
仓库内还有的内容：
(1) 项目源码（作为部署链接失效兜底）；
(2) 线上部署访问URL；
(3) 数据库、接口、AICodeReview截图包；
(4) 文档：README.md、prompt_log.md、API 文档；
(5) 项目演示录屏
(6) 个人实训总结报告
(7) 组队附加：分工表、Git贡献统计截图。
 准则：别人看了你的文档，能清楚知道你这个项目做了什么，可以复现！